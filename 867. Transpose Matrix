class Solution:
    def transpose(self, matrix: List[List[int]]) -> List[List[int]]:
        res = []
        n = len(matrix)
        m = len(matrix[0])
        for i in range(m):
            row = []
            for j in range(n):
                row.append(matrix[j][i])
            res.append(row)
        return res
