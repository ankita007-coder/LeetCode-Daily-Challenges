class Solution:
    def arrayStringsAreEqual(self, word1: List[str], word2: List[str]) -> bool:
        st1,st2 = "",""
        for i in word1:
            st1+=i
        for i in word2:
            st2+=i
        return st1==st2
